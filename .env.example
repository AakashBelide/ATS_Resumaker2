# Copy to .env and fill in. .env is gitignored (never commit real keys).

# ------------------------------------------------------------------ core config
# Environment tag: local | vm | ci (informational; drives a couple of defaults).
RESUMAKER_ENVIRONMENT=local

# LLM engine for cognitive stages: claude | anthropic | gemini.
#  - claude   = Claude CLI (subscription, $0 tokens). Great locally; NOT on a headless VM.
#  - anthropic= Anthropic API (credits). The recommended engine for a deployed VM.
RESUMAKER_DEFAULT_PROVIDER=claude
# CLI-first everywhere with automatic failover: keep `claude` primary and set a paid API
# fallback the provider layer switches to when the CLI fails / is rate-limited (needs that
# provider's key below). Blank = no fallback. One of: anthropic | gemini.
RESUMAKER_FALLBACK_PROVIDER=
# Cloud auth for the Claude CLI (headless): a subscription OAuth token from `claude setup-token`.
# Preferred over ANTHROPIC_API_KEY so the CLI stays on the $0 subscription in the cloud.
CLAUDE_CODE_OAUTH_TOKEN=

# API auth: when set, every /v1 request must present this token
# (Authorization: Bearer <token> or X-API-Key). REQUIRED on any exposed VM.
RESUMAKER_API_TOKEN=

# ------------------------------------------------------------------ LLM secrets
# Needed when DEFAULT_PROVIDER=anthropic (or for parity tests). Conventional name.
ANTHROPIC_API_KEY=

# Gemini API (hard-capped at $5 by the cost guard). Only for `--semantic gemini`,
# embeddings, and Gemini fallback; the default pipeline does not need it.
GEMINI_API_KEY=

# ------------------------------------------------------------------ serverless (dual-mode)
# Everything below defaults to the LOCAL path; set the cloud values only when deploying to
# Cloud Run. Secrets (Turso token) live ONLY here in .env (gitignored), never committed.
#
# DB: local SQLite file (default) OR hosted Turso/libSQL. Set both to use Turso.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
# Job queue: run pipelines in-process (default) OR enqueue Cloud Tasks -> the worker service.
RESUMAKER_JOB_QUEUE=inprocess          # inprocess | cloud_tasks
RESUMAKER_WORKER_URL=                  # worker base URL (Cloud Tasks HTTP target)
RESUMAKER_TASKS_QUEUE=resumaker-pipeline
RESUMAKER_GCP_PROJECT=
RESUMAKER_GCP_REGION=
# Artifact store: local disk (default) OR GCS (signed URLs).
RESUMAKER_ARTIFACT_BACKEND=local       # local | gcs
RESUMAKER_GCS_BUCKET=

# ------------------------------------------------------------------ watchlist scheduler
# When true, the API polls the watchlist every N minutes, dedupes, and notifies on new
# preference-matching postings (never runs the pipeline or applies). In cloud, leave this
# false and let Cloud Scheduler hit /v1/worker/ingest-tick instead.
RESUMAKER_SCHEDULER_ENABLED=false
# Clean public boards (Greenhouse/Lever/Ashby) poll often; Workday (Akamai, throttles) daily.
RESUMAKER_SCHEDULER_INTERVAL_MINUTES=60
RESUMAKER_SCHEDULER_WORKDAY_INTERVAL_MINUTES=1440
# Optional: POST a JSON digest of new postings to this webhook (Slack/Discord/etc.).
RESUMAKER_NOTIFY_WEBHOOK=

# Email digest of new on-target postings. Two ways to send (pick one) - all secrets live ONLY
# here in .env (gitignored), never committed.
#
# (A) RECOMMENDED - Resend API (send-only key, can't read your mailbox; revocable):
#   RESUMAKER_RESEND_API_KEY=re_...
#   RESUMAKER_NOTIFY_FROM  defaults to onboarding@resend.dev (works on the free tier without a
#     verified domain). Verify a domain in Resend to send from your own address.
#   RESUMAKER_NOTIFY_TO    on the FREE tier without a verified domain, Resend only delivers to
#     YOUR Resend account email - set this to that address for now (verify a domain to send
#     to any recipient, e.g. your NEU inbox).
RESUMAKER_RESEND_API_KEY=
#
# (B) OPTIONAL SMTP fallback - only if you are NOT using Resend (e.g. a Gmail App Password).
# Leave these commented/blank when using Resend; the code ignores them if RESEND key is set.
# RESUMAKER_SMTP_HOST=
# RESUMAKER_SMTP_PORT=587
# RESUMAKER_SMTP_USER=
# RESUMAKER_SMTP_PASS=
#
# Shared (used by whichever sender is configured):
RESUMAKER_NOTIFY_TO=
RESUMAKER_NOTIFY_FROM=

# ------------------------------------------------------------------ deploy (Caddy)
# Set to your hostname for automatic HTTPS (e.g. resumaker.example.com); leave blank
# for plain HTTP on :80 (local/VM testing).
DOMAIN=

# ------------------------------------------------------------------ Affinda (OPTIONAL)
# Independent industry parse oracle (validation only, not in the pipeline).
# Free tier: https://app.affinda.com  (region-scoped - set BASE_URL to your account's region).
AFFINDA_API_KEY=
AFFINDA_WORKSPACE=
AFFINDA_DOCUMENT_TYPE=
#   https://api.affinda.com (APAC) | https://api.us1.affinda.com (US) | https://api.eu1.affinda.com (EU)
AFFINDA_BASE_URL=
AFFINDA_COLLECTION=
